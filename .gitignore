.DS_Store
__pycache__/
*.pyc

# Python build / test artifacts (pip/pipx/setuptools, uv, pytest)
build/
dist/
*.egg-info/
.venv/
uv.lock
.pytest_cache/

# Personal per-machine Claude Code settings (committed slot is .claude/settings.json)
.claude/settings.local.json

# Serena MCP project-index cache — local-only, never meant to be committed.
.serena/
